<Application x:Class="forthePL.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:forthePL"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <ControlTemplate x:Key="errorTemplate">
            <Border BorderBrush="Red" BorderThickness="2">
                <Grid>
                    <AdornedElementPlaceholder/>
                    <Image Source="C:\Users\aviga\Pictures\Mini Project\ErrorMessage.png"  Stretch="Fill"
                           Height="15" Width="15" Margin="0,0,8,0" HorizontalAlignment="Right"
                           VerticalAlignment="Center">
                    </Image>
                    <TextBlock Text="{Binding ErrorContent}" Foreground="Red"
                    VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,25,0"/>
                </Grid>
            </Border>
        </ControlTemplate>


        <!--<Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="Red"></Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Blue"></Setter>
                </Trigger>
            </Style.Triggers>
        </Style>-->

    </Application.Resources>
</Application>
